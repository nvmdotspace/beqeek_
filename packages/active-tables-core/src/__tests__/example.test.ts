import { describe, it, expect } from 'vitest';

describe('Active Tables Core - Example Test', () => {
  it('should pass basic test', () => {
    expect(true).toBe(true);
  });

  it('should perform arithmetic operations', () => {
    const sum = 2 + 2;
    expect(sum).toBe(4);
  });
});
